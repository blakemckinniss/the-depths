# Sync Beads with Git

Sync the beads database with the git remote:

```bash
bd sync
```

This commits any local changes to issues and pulls updates from the remote. Use after completing work or before starting a new session.
