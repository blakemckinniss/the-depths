# Ready Issues

Show issues that are ready to work on (no blocking dependencies):

```bash
bd ready
```

For machine-readable output:

```bash
bd ready --json
```

Pick the highest priority issue and start working on it with `bd update <id> --status in_progress`.
